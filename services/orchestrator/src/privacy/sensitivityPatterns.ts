/**
 * Re-export from @har/shared — single source of truth.
 * This file exists for backward compatibility with existing orchestrator imports.
 */
export { SENSITIVITY_PATTERNS, SENSITIVE_KEYWORDS } from "@har/shared";
